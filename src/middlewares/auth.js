/*const authAdmin = (req,res,next)=>{
    const token = "secret";
    const isAuthenticated = token === "secret";
    if(!isAuthenticated){
        res.status(401).send('Unauthorized');
    }else{
        next();
    }
}

module.exports = authAdmin;*/